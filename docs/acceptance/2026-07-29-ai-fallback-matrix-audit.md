# Cross-technique AI-off/failure/fallback matrix audit

Fresh command with D: basetemp:

```text
25 passed in 0.23s
AI_FALLBACK_EXIT_CODE=0
```

The matrix covers cross-technique registration and deterministic AI-off,
preprocessing fault, failed decision, and fallback contracts. It does not
establish scientific model quality, calibration, vendor mapping semantics,
restarted-GUI visual approval, or release authorization. No test data was
deleted or migrated.
