# Native all-mode route acceptance

Date: 2026-07-30

The current checkout has automated native Windows Qt route evidence for all real
technique modes plus synthetic Joint and IR mapping boundaries.

Evidence:

- The post-prefix native matrix returned `17 passed, 15 warnings in 320.08s`,
  exit code `0`.
- Coverage is DSC `3`, SAXS `3`, WAXS `3`, IR `2`, NMR `4`, synthetic Joint
  `1`, and synthetic IR mapping `1`.
- The D:-isolated capture directory
  `D:\PolyNexus_native_all_routes_capture_20260729_post_prefix` contains 68
  PNGs: Results, Gallery, History, and Editor for each of the 17 cases.
- Every case exercised the real Editor Export action with the no-Origin
  PackageExporter fallback.

This acceptance proves route construction, shared-workbench surfaces, live
labels, and fallback export artifacts. It does not close human visual review,
IR vendor/coordinate/ROI semantics, NMR solid-C assignment correctness, Joint
conflict interpretation, SAXS diagnostic/validation meaning, or final release
approval.

No source datasets were changed and no test data was deleted or migrated.
