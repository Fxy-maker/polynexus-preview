# Windows-native all-route capture retry

The fresh current-checkout Windows-native Qt harness returned:

```text
17 passed, 15 warnings in 370.95s (0:06:10)
NATIVE_GUI_EXIT_CODE=0
```

It produced `68` PNG captures under
`D:\PolyNexus_native_all_routes_capture_20260729_retry`, covering four
surfaces per route and the no-Origin PackageExporter fallback. Inspected
representatives show SAXS temperature diagnostic-only evidence, IR mapping's
`Not confirmed yet` boundary, editable but crowded NMR solid-C labels, and
Joint `PA6-A` identity with 2 errors/2 warnings.

This is fresh native route/capture evidence, not unlocked interactive human
visual acceptance or scientific/release authorization. No test data was
deleted or migrated.
