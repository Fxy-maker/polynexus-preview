# Current HEAD Native Non-SAXS Walkthrough Design

Run the existing native route harness against the current checkout with SAXS
excluded, an external capture directory, and `QT_QPA_PLATFORM=windows`.
Treat route construction, visible labels, and export fallback as software
evidence. Treat scientific decisions and synthetic fixture review records as
separate human-owned boundaries.
