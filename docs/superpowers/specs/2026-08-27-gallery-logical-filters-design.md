# Gallery Logical Filters

The evidence gallery will accept optional case-insensitive `technique`,
`group`, and `role` filters against `FigureIndexEntry` fields. Filtering occurs
before SVG/document resolution. Existing no-argument behavior is unchanged.
